<%= form_tag(shorten_path) do %>

  <div class="field">
    <%= label_tag :long_url %><br />
    <%= text_field_tag :long_url %>
  </div>

  <div class="actions">
    <%= submit_tag 'Submit' %>
  </div>
<% end %>
